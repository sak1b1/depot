<p id="notice"><%= notice %></p>


<div class="card">
  <img src=" <%= @product.image_url %>" alt="Avatar" style="width:100%" class="img" height="100px" width="100px">
  <div class="container">
    <h1>
      <strong>Title:</strong>
      <%= @product.title %>
    </h1>

    <p>
      <strong>Description:</strong>
      <%= @product.description %>
    </p>

    <p>
      <strong>Image url:</strong>
      <%= @product.image_url %>
    </p>

    <p>
      <strong>Price:</strong>
      <%= number_to_currency(@product.price) %>
    </p>
  </div>
</div>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>

