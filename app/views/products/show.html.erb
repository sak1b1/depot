<p id="notice"><%= notice %></p>


<div class="card">
  <img src=" <%= @product.image_url %>" alt="Avatar" style="width:100%" class="img">
  <div class="container">
    <h1>
      <strong>Title:</strong>
      <%= @product.title %>
    </h1>

    <p>
      <strong>Description:</strong>
      <%= @product.description %>
    </p>

    <p>
      <strong>Image url:</strong>
      <%= @product.image_url %>
    </p>

    <p>
      <strong>Price:</strong>
      <%= @product.price %>
    </p>
  </div>
</div>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>

